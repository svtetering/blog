---
title: Recent Posts
layout: main
pagination:
    data: collections.posts
    size: 5
    alias: posts
    reverse: true
ogtype: website
---

{% for post in posts %}
<div class="listed-post">
    <header>
        <a href="{{ post.url }}" class="post-title">{{ post.data.title | escape }}</a>
        <p class="post-meta">{{ post.date | date: "%B %d, %Y"}}{% if post.data.edited %} <span class="edited">(edited {{post.data.edited | date: "%B %d, %Y"}})</span>{% endif %}</p>
    </header>
    <p class="excerpt">{% capture ex %}{% excerpt post %}{% endcapture %}{{ ex | escape }}</p>
    <p><a href="{{ post.url }}">Read ></a></p>
</div>
{% endfor %}

<p>
    {% if pagination.href.previous %}
    <a href="{{ pagination.href.previous }}" class="pagination-button-left">&lt;- Previous</a>
    {% else %}
    <a class="pagination-button-left"><s>&lt;- Previous</s></a>
    {% endif %}
    {% if pagination.href.next %}
    <a href="{{ pagination.href.next }}" class="pagination-button-right">Next -&gt;</a>
    {% else %}
    <a class="pagination-button-right"><s>Next -&gt;</s></a>
    {% endif %}
</p>